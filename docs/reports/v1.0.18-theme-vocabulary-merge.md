# Theme Vocabulary Merge (bug_004 resolution)

## Report Version
v1.0.18

## Purpose
Execute the re-sealed v1.0.18 artifact (generated under the Prompt Artifact Contract v1.0.0, re-sealed 2026-04-24 with Edits #1/#2 and M1/M2 from the memo-pass review). Resolve bug_004 — the parallel `Theme` vocabularies in `reflection.tsx` and `structural-primitives.ts` — by merging them into one canonical `Theme` + `themePatterns` + `detectThemes` under primitives. First pass where byte-identical harness output is explicitly released for the `reflection` field.

## Trigger
User prompts **"Create a sealed execution artifact for v1.0.18"** → memo-pass review → **"yes please"** (re-seal authorization with Edits #1/#2 + M1/M2) → **"execute v1.0.18"** (this pass) — all on 2026-04-24. Third complete sealed-artifact loop under the Prompt Artifact Contract.

## State Observed (pre-execution)

- `reflection.tsx` owned a local `Theme` union (~lines 31-39), local `themePatterns` (narrower vocabulary, ~lines 41-50), and local `detectThemes` function (~lines 52-65)
- `structural-primitives.ts` owned `CapabilityTheme` type + `capabilityThemePatterns` (strict-superset vocabulary) + `detectCapabilityThemes`
- `synthesizers.ts` imported `CapabilityTheme` and `detectCapabilityThemes`; three template Records typed `Record<CapabilityTheme, string>`
- External harness mirrored BOTH theme tables separately with two detector functions
- Pre-execution baseline at `C:/Users/JSEer/AppData/Local/Temp/v1017-harness.out` verified byte-identical before starting (step 2 of Execution Instructions confirmed reproducibility with diff exit 0)
- All four governance contracts composing

## Action Taken

Three repo files + one external harness file modified.

### 1. `app/session/flow/structural-primitives.ts` — three renames + two comment updates
- `CapabilityTheme` → `Theme`
- `capabilityThemePatterns` → `themePatterns` (vocabulary content unchanged — already canonical)
- `detectCapabilityThemes` → `detectThemes` (function body unchanged apart from references)
- Section header updated: *"Theme primitives (v1.0.11 capability / v1.0.5 reflection, merged in v1.0.18)"*
- bug_004 deferral note replaced with resolution note pointing at the v1.0.18 sealed artifact's Decision 5

### 2. `app/session/flow/synthesizers.ts` — import + Record type updates + one call-site rename
- Import `type CapabilityTheme` → `type Theme`; `detectCapabilityThemes` → `detectThemes`
- Three Records (`capabilityActionByTheme`, `capabilityStrengthByTheme`, `capabilityValueByTheme`) rekeyed from `Record<CapabilityTheme, string>` → `Record<Theme, string>`; keys and values unchanged
- `synthesizeCapability` body: `const themes = detectCapabilityThemes(joined)` → `const themes = detectThemes(joined)`

### 3. `app/session/flow/reflection.tsx` — import added, three local declarations deleted, SCL comment updated
- Added: `import { type Theme, themePatterns, detectThemes } from './structural-primitives'`
- Deleted: local `type Theme` union (8-member declaration)
- Deleted: local `const themePatterns` narrower-vocabulary table
- Deleted: local `function detectThemes` detector
- `capabilityBullet` and `patternBullet` Records unchanged; they now resolve the imported `Theme` type
- `synthesizeReflection` body unchanged; `detectThemes` call now resolves to the imported canonical function
- SCL comment updated: bug_004 resolution noted; the bounded module-boundary exception explicitly documented (reflection imports directly from primitives; no other flow screen may follow this pattern; resolution target named)

### 4. `C:/Users/JSEer/AppData/Local/Temp/visionair-eval.mjs` — harness unified
- Replaced reflection's narrower `themePatterns` with the canonical (capability-superset) vocabulary; added "v1.0.18: canonical themePatterns" marker comment
- Deleted the duplicate `capabilityThemePatterns` constant (previously lived near line 294)
- Deleted the `detectCapabilityThemes` function (previously lived near line 356)
- Updated the `synthesizeCapability` call site: `detectCapabilityThemes(joined)` → `detectThemes(joined)`
- Extended the harness header sync comment with *"themePatterns unified in v1.0.18 (bug_004 resolved)"*

### Explicit non-scope (preserved)
- `app/session/page.tsx` — unmodified (no import or composition change needed)
- All other flow screens (`transformation.tsx`, `blueprint.tsx`, `ideal-user.tsx`, etc.) — unmodified
- All four governance contracts — unmodified
- `.claude/CLAUDE.md` — unmodified
- `docs/system-state/` — no new entry required (v1.0.18 honored the v1.0.15 roadmap)

## Result
- One canonical `Theme` + `themePatterns` + `detectThemes` lives in `structural-primitives.ts`
- `reflection.tsx` imports them directly (bounded module-boundary exception, explicitly scoped in the SCL comment and Decision 5)
- `synthesizers.ts` uses the unified names
- Harness mirrors a single canonical vocabulary; `capabilityThemePatterns` and `detectCapabilityThemes` no longer exist anywhere
- bug_004 fully closed

## Validation

### V1 — TypeScript compilation
`npx tsc --noEmit` returned zero errors, zero output (beyond npm version notices, filtered). **Pass.**

### M2 (Step 7) — app/harness parity check
Grep-extracted the canonical `themePatterns` from both `app/session/flow/structural-primitives.ts` and `C:/Users/JSEer/AppData/Local/Temp/visionair-eval.mjs`. The two tables are content-identical: same 8 theme names, same regex source strings per theme, same regex flags, same pattern order within each theme. Only difference is the TypeScript type annotation (`: Record<Theme, RegExp[]>`) which is absent in the JS harness. **Parity confirmed before harness re-run.**

### V2 — Capability output byte-identical
All 10 personas' `blueprint.capability` field byte-identical between `v1017-harness.out` and `v1018-harness.out`. **Pass.**

### V3 — All non-theme-consuming fields byte-identical
All other top-level fields (`transformation_before`, `transformation_after`, and every blueprint sub-field except `reflection` and `blueprint.capability` — both covered above) byte-identical across all 10 personas. **Pass.**

### V4 — Reflection diffs classified and bounded (with M1 classification)

**Diff scope: 1 line, 1 persona (P1).**

```
diff v1017-harness.out v1018-harness.out →
5c5
<       "There is a recurring pattern of turning scattered material into a coherent structure others can follow.",
---
>       "There is a recurring pattern of making hidden structure visible so decisions become easier.",
```

Only `reflection[1]` (the `patternBullet` slot) of persona P1 shifted. Every other persona (P2–P10) shows byte-identical reflection output.

| Persona | Classification | Triggering vocabulary | Judgment |
|---|---|---|---|
| P1 Tech/systems builder | **Reordered** | `\bproduct/i` now matches "productivity" (building +1); `\bdebug/i` now detected by analysis vocabulary (analysis +1); `\bbottleneck/i` now detected by problemSolving (problemSolving +1) | **Improvement** |
| P2–P10 | None | n/a | n/a |

**Classification breakdown for P1:**
- Scoring under old (narrower) vocabulary: building=2 (build + developer), structure=1 (systems), guiding=1 (helps), analysis=1 (pattern), problemSolving=1 (debug). Sort stable-descending → `themes = ['building', 'structure', 'guiding', 'analysis', 'problemSolving']`. Bullet 1 used `capabilityBullet['building']`; Bullet 2 used `patternBullet['structure']`.
- Scoring under canonical (broader) vocabulary: building=3 (build + developer + "productivity" via `\bproduct`), analysis=2 (pattern + debug), problemSolving=2 (debug + bottleneck), structure=1, guiding=1. Sort stable-descending → `themes = ['building', 'analysis', 'problemSolving', 'structure', 'guiding']`. Bullet 1 still uses `capabilityBullet['building']` (themes[0] unchanged); Bullet 2 now uses `patternBullet['analysis']` (themes[1] shifted from 'structure' to 'analysis').

**Classification:** Reordered — top theme unchanged, second theme displaced by a now-higher-scoring peer. Not Additive (no new theme appeared in themes[0–1]); not Substituted (themes[0] held).

**Judgment — strong improvement:** P1's seed input describes debugging distributed systems and surfacing bottleneck patterns — core analysis work. The old `patternBullet['structure']` ("turning scattered material into a coherent structure others can follow") was thematically adjacent but less accurate. The new `patternBullet['analysis']` ("making hidden structure visible so decisions become easier") is substantially more accurate to what P1's tool actually does: surfacing the system's internal structure so engineers can reason about it under time pressure.

**V4 passes.** Single diff. Cleanly classifiable. Root cause identifiable as specific canonical-vocabulary gains. No diff requires flagging for v1.0.19+ polish.

### V5 — No stale references
```
grep -rE 'CapabilityTheme|capabilityThemePatterns|detectCapabilityThemes' app/
→ zero matches
```
**Pass.**

### V6 — Reflection.tsx has no local declarations
```
grep -E '^(type Theme|const themePatterns|function detectThemes)' app/session/flow/reflection.tsx
→ zero matches
```
**Pass.**

### Non-mandatory observations
- **Reflection diff count by classification:** Additive 0 / Reordered 1 / Substituted 0. The lightest possible outcome — minimal behavior change for the broadest possible theoretical vocabulary expansion
- **No `\border\b` false positive observed.** The dropped negative lookahead (reflection's old `\border(?!\s+from)\b` → canonical `\border\b`) did not produce a false match in any of the 10 persona inputs
- **Line-count deltas:** structural-primitives.ts 410 → 410 (renames only, vocab unchanged); synthesizers.ts 273 → 273 (renames only); reflection.tsx 440 → 401 (−39 from deleted Theme/themePatterns/detectThemes blocks); harness minor net reduction from deduplicated theme table
- **Both overlap-related tables confirmed preserved:** `emotionalTranslations` (reflection) vs `beforeTranslations` (primitives) and `idealUserBlockerPatterns` (primitives) vs `beforeTranslations` — all separate. Different bugs, different sentences, different output templates, correctly not in scope for this pass

### Disk truth
- `app/session/flow/structural-primitives.ts` — modified (three renames, two comment updates)
- `app/session/flow/synthesizers.ts` — modified (import, three Record type annotations, one call site)
- `app/session/flow/reflection.tsx` — modified (import added, three local declarations deleted, SCL comment updated)
- `C:/Users/JSEer/AppData/Local/Temp/visionair-eval.mjs` — modified (theme table unified, capability duplicate deleted, call site updated, header extended)
- `docs/reports/v1.0.18-theme-vocabulary-merge.md` — written (this file)
- Auto-memory pointers — to be updated at end of pass
- No other file touched

---

## Re-Entry Validation (per Prompt Artifact Contract §"Re-Entry Validation Rule")

1. **Compare execution against the re-sealed artifact.** All four Changes applied exactly as specified. Edits #1, #2, M1, M2 from the re-seal all honored: Decision 3 softened the improvement claim (though in this pass the one observed diff IS an unambiguous improvement, the softened framing stands as intended); Decision 5 formalized the module-boundary exception; V4 required M1 classification (Additive/Reordered/Substituted) and the single diff was classified as Reordered; Step 7 M2 parity check confirmed app/harness identical before harness re-run. **No deviations.**

2. **Verify scope discipline.** Three repo files + external harness modified, matching the re-sealed artifact's §"Scope" exactly. No other repo file touched.

3. **Verify adherence to all active contracts.**
   - Report Contract: nine required sections present; truth rule honored (every validation backed by observed output); filename follows convention. ✓
   - SCL: reflection output now uses broader vocabulary that more accurately translates P1's input from emotional/experiential language into operational meaning (Rule 3). ✓
   - HLMC: Required Output Block present in §"Readiness / Next Move". ✓
   - Prompt Artifact Contract: re-sealed artifact generated first, user approval recorded ("execute v1.0.18"), human gate observed; Re-Entry Validation applied before this report is considered final. ✓

4. **Validate outcomes match actual behavior.** Every V1–V6 claim traceable to a specific tool invocation output (tsc, diff, grep, node). No pre-written claims.

5. **No fabricated claims.** Cross-checked. The P1 classification walk-through traces scoring algebra against the actual vocabulary content and matches the observed diff content exactly.

6. **Determine if the next move remains correct.** With bug_004 closed and the synthesis substrate fully consolidated (v1.0.17) + merged (v1.0.18), the remaining work is bucket-2 user-trust fixes. v1.0.19 as the first of those (bug_011 scope denylist — the simplest and most surgical) is the correct next move per the v1.0.15 system-state roadmap. **Next move unchanged; clarity improved.**

**Re-Entry Validation passes.** Report approved to write.

---

## Calibration Note

The v1.0.9 baseline of 3.36/5 was measured under pre-merge reflection behavior. Post-v1.0.18, P1's reflection output shifts (Reordered, themes[1] changed); P2–P10 unchanged. A formal calibration rerun is not performed in this pass; that would belong in a dedicated calibration pass after bucket-2 lands. Qualitative assessment for P1 only: the new patternBullet is a clear improvement in input-fit; if calibration were rerun today, P1's Structural Cohesion sub-score would likely nudge upward rather than down. Other personas' scores should be unchanged.

Post-v1.1.0 user testing will be the next authoritative calibration event.

---

## Readiness / Next Move

### Current System State

**Working:**
- Four composing governance contracts active
- One canonical `themePatterns` in `structural-primitives.ts`; reflection + capability both consume it
- Five synthesizer functions across two modules (synthesizers.ts + reflection.tsx)
- `synthesizeReflection` remains in `reflection.tsx` as a bounded module-boundary exception, formally scoped and named for future extraction
- TypeScript clean; harness confirms capability + all non-reflection fields byte-identical; reflection diff minimal and explainable
- bug_004 closed; three full sealed-artifact loops completed without drift (v1.0.16, v1.0.17, v1.0.18)

**Not working / weak:**
- Four bucket-2 user-trust findings from the v1.0.13 ultrareview still pending:
  - bug_011 — `detectVersionOneScope` over-matches discourse markers like `no doubt`, `no problem`, `no way`
  - bug_014 — `state.reflection` write-only; refine/recalibrate CTA silently discards user input
  - bug_006 — `ideal-user.tsx` and `version-one.tsx` preview cards show raw user text while blueprint shows synthesized text
  - bug_005 — `synthesizeCapability` flattens 5 semantically distinct answers before detection; Q5 preference outweighs Q2 external-demand signal
- Pattern-quality polish from v1.0.16 (P1 blocker vocabulary extension, context-pattern precision) still outstanding
- `synthesizeReflection` still lives in reflection.tsx (bounded exception); full extraction deferred to v1.0.21 or similar

**Bottleneck:**
The synthesis substrate is now architecturally complete — one vocabulary home, five synthesizers, four composing contracts, three clean sealed-artifact loops. The remaining gating concern before v1.1.0 user testing is artifact trust at the UI surface. bug_011 is the smallest, most surgical, lowest-risk bucket-2 fix (denylist extension, no calibration rerun required), making it the cleanest first pass in the bucket-2 sequence.

### Highest-Leverage Next Move

**Execute v1.0.19 — bug_011 scope denylist extension.** Extend the 6-token denylist in `detectVersionOneScope` (`app/session/flow/structural-primitives.ts`) to filter additional common English discourse markers that follow `no`/`without` (e.g., `doubt`, `problem`, `time`, `way`, `question`, `real`, `good`, `reason`, `clue`, `rush`, `surprise`, `offense`, `worries`, etc.). No calibration rerun required; harness should remain byte-identical for all 10 personas because no current persona's versionOne input triggers these false-positive patterns.

### Exact Next Step

Sealed-artifact loop recommended. User invokes **"Create sealed execution artifact for v1.0.19"**. Sketch:

1. In `structural-primitives.ts` `detectVersionOneScope`: extend the exclusion denylist from 6 tokens to ~20 covering common discourse markers
2. Harness mirror: apply identical extension to keep app/harness parity (M2 pattern)
3. TypeScript check + harness re-run; expect byte-identical output across all 10 personas (no persona's current versionOne input has false-positive exclusions)
4. Non-harness test: construct 6-8 synthetic inputs exercising the new denylist (e.g., "no doubt this will work", "no problem at all") and confirm they correctly fall through the exclusion branch to a real scope detector or to the "not yet explicitly bounded" fallback
5. Write v1.0.19 report documenting the extended denylist + synthetic-input validation

### Why This Move

- **Impact relative to alternatives.** bug_011 is the smallest, lowest-risk bucket-2 fix. It's a denylist extension on the most user-visible blueprint card (Section 6, First Buildable Version) where false positives fabricate user-attributed scope choices. Direct user-trust impact before v1.1.0.
- **What it unlocks.** Lowers risk for the three remaining bucket-2 fixes (bug_014 product decision, bug_006 UI plumbing, bug_005 calibration-shifting) by clearing the simplest one first.
- **Why sequenced now.** bug_011 is surgical and doesn't require calibration rerun; the next three bucket-2 items each have either a product decision or a calibration-shift component. Clearing the easy one first reduces cognitive load on the harder ones.

### What Not To Do Yet

1. **bug_014, bug_006, bug_005** — all required before v1.1.0; not blockers on v1.0.19. Each has its own risk profile (bug_014 is a product decision; bug_006 requires UI plumbing + matching the transformation-screen precedent; bug_005 requires per-answer weighting + calibration rerun). Sequence after bug_011.
2. **Pattern-quality polish from v1.0.16** (P1 blocker vocabulary, `incidents`/`on-call`/`gets paged` regex precision) — should group with bug_011 (same file, same pattern class) OR with bug_005 (also touches synthesizer pattern tables). Defer the decision until v1.0.19 scope is sealed.
3. **Full extraction of `synthesizeReflection` into `synthesizers.ts`** (closes the v1.0.18 module-boundary exception) — still on the backlog; still correctly sequenced after bucket-2 clears.
4. **Retrofit of pre-v1.0.15 reports** — not rewriting history.
5. **New `docs/system-state/` entry** — v1.0.18 honored the v1.0.15 roadmap; no supersession needed.

---

## Final Statement

bug_004 resolved. One canonical theme vocabulary. Reflection and capability share it. The single observed reflection diff is an unambiguous improvement on P1's input-fit. Three sealed-artifact loops now completed end-to-end without drift.

The synthesis substrate is architecturally complete. The next phase is user-trust polish before v1.1.0.

v1.0.19 is cleared to begin.
