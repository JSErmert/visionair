# Preview Parity (bug_006 closure)

## Report Version
v1.0.23

## Purpose
Close bug_006 from the v1.0.13 ultrareview. The Ideal User and Version One edit screens rendered the user's raw textarea input as a preview card, while the blueprint rendered the synthesized output. That asymmetry created a "the blueprint changed what I said" trust gap on two of the three synthesizer-fed blueprint sections. This pass applies the `transformation.tsx:177-209` preview pattern (memoized synthesis via `useMemo`, structured bullet rendering) to both edit screens so their previews reflect the same synthesized bullet form rendered on the blueprint. No synthesizer logic change; harness byte-identical.

## Trigger
User prompt **"execute v1.0.23"** on 2026-04-24, authorizing execution of the re-sealed v1.0.23 artifact (re-sealed same day with tightening edits T1–T4 incorporated: V5a grep specificity, V5b renaming from "integration logic test" to "structural shape smoke test", V5c fallback-copy preservation additions, purpose/SCL-Rule-5 wording precision). Claude-side memo-pass returned Pass verdict before Phase 5 authorization. Third bucket-2 fix under the seven-layer governance stack (six composing contracts + `docs/v1-1-0-completion-guidance.md` terminal directive).

## State Observed (pre-execution)
- `app/session/flow/ideal-user.tsx` pre-v1.0.23: 81 lines; canContinue-gated preview rendered `<p>{value}</p>` raw text
- `app/session/flow/version-one.tsx` pre-v1.0.23: 72 lines; same structural pattern as ideal-user
- `transformation.tsx:177-204` reference pattern: `useMemo` with synthesizer call; `<ul><li>` bullet rendering
- Blueprint SectionCard bullet styling: `text-base leading-7 text-black/80` (v1.0.14 widened contract)
- Pre-execution baseline verified: `diff v1018-harness.out v1023-baseline-verify.out` exit 0
- Seven-layer governance active: Report, SCL, HLMC, PAC, CASS, ARRSC + `docs/v1-1-0-completion-guidance.md`
- Bucket-2 status: bug_011 ✅, bug_014 ✅, bug_006 ← this pass, bug_005 remaining

## Action Taken

Six edits across two repo files. Zero synthesizer logic changed. Zero harness change. No new system-state entry.

### 1. `app/session/flow/ideal-user.tsx` — three edits
- **Edit 1a** — added imports: `import { useMemo } from 'react'` and `import { synthesizeIdealUser } from './synthesizers'`
- **Edit 1b** — added memoized bullets computation after canContinue: `const bullets = useMemo(() => synthesizeIdealUser(value), [value])`
- **Edit 1c** — replaced `<p className="text-base leading-7 text-black/80">{value}</p>` with a structured `<ul className="space-y-2">` + `<li key={i} className="text-base leading-7 text-black/80">• {bullet}</li>` mapping over `bullets`. Empty-state fallback copy preserved verbatim.

### 2. `app/session/flow/version-one.tsx` — three edits
Structurally identical to ideal-user.tsx changes, using `synthesizeVersionOne` instead. Fallback copy preserved verbatim.

### 3. Wrote `docs/reports/v1.0.23-preview-parity.md` (this file).

### 4. Updated auto-memory
- `visionair_project_state.md` — pointer refreshed to v1.0.23 report, bug_006 closed, v1.0.24 bug_005 as final bucket-2 pass
- `MEMORY.md` — index line refreshed

### Out-of-scope preserved
- `synthesizers.ts`, `structural-primitives.ts` — no synthesizer logic change
- All other flow screens — untouched
- `page.tsx` — untouched (session state + blueprint composition unchanged)
- Six composing contracts + terminal directive — untouched
- `docs/system-state/v1.0.20-authority-supersession-adopted.md` — remains authoritative
- External harness — untouched (UI change; harness doesn't model edit-screen render)
- `.claude/CLAUDE.md` — untouched

## Result
- bug_006 closed — edit-screen previews on Ideal User and Version One now render the same synthesized bullet form the blueprint will render
- Harness byte-identical to v1.0.18 baseline (no synthesizer touched)
- TypeScript clean
- Three bucket-2 bugs closed (bug_011, bug_014, bug_006); one remaining (bug_005)
- Preview-to-blueprint trust gap eliminated on two surfaces

## Validation

### V1 — TypeScript compilation
`npx tsc --noEmit` returned zero errors, zero output. `synthesizeIdealUser` and `synthesizeVersionOne` both return `string[]`; `.map((bullet, i) => ...)` is type-safe; `useMemo` with `[value]` dep is standard.

**Pass.**

### V2 — Baseline verified pre-execution
`diff v1018-harness.out v1023-baseline-verify.out` exit 0. Pre-execution state reproduced v1.0.18 baseline.

**Pass.**

### V3 — App/harness parity
**N/A for this pass.** UI-only change. No harness mirror involved.

### V4 — Harness byte-identical post-execution
`diff v1018-harness.out v1023-harness.out` exit 0. Post-execution harness output byte-identical to baseline. Confirms no synthesizer touched.

**Pass.**

### V5a — Static edit verification (with specification-accuracy note)

Actual grep counts observed:
- `synthesizeIdealUser` in `ideal-user.tsx`: **2** (import + useMemo call)
- `synthesizeVersionOne` in `version-one.tsx`: **2** (import + useMemo call)
- `useMemo` in `ideal-user.tsx`: **2** (react import + call)
- `useMemo` in `version-one.tsx`: **2** (react import + call)
- `bullets.map` in `ideal-user.tsx`: **1** (render)
- `bullets.map` in `version-one.tsx`: **1** (render)

**Specification-accuracy observation:** the re-sealed artifact's V5a spec said "each grep must return 1." That phrasing was imprecise — it treated all three anchors as render-only, but the synthesizer and `useMemo` anchors appear twice in each file (once at the import line, once at the usage site), which is the correct designed shape. The anchor counting should have been: `synthesizeIdealUser`/`synthesizeVersionOne` = 2, `useMemo` = 2, `bullets.map` = 1.

**Intent vs literal spec:** V5a's intent was "confirm the correct synthesizer import is present AND useMemo is present AND bullets.map is present in the render." All three presence conditions verified. The literal "count must equal 1" phrasing was wrong for import+usage anchors. V5a passes by intent.

**Pass by intent.** Note: this is a specification-accuracy gap to watch in future memo-passes when the artifact specifies grep counts on anchors that appear in both import and usage positions. Flagging as a governance observation, not a blocker on this pass.

### V5b — Structural shape smoke test
Throwaway Node script at `C:/Users/JSEer/AppData/Local/Temp/v1023-synthetic.mjs` confirmed:

| Test | Array? | Length ≥ 1? | All non-empty strings? | Result |
|---|---|---|---|---|
| `synthesizeIdealUser` on P5-like input ("founder of a 15-50 person startup...") | ✅ | ✅ (length 3) | ✅ | PASS |
| `synthesizeVersionOne` on P1-like input ("A CLI that ingests a single trace file...") | ✅ | ✅ (length 3) | ✅ | PASS |

Script exit code 0. Shape guarantees hold for representative inputs.

**Pass.**

### V5c — Directive compliance + fallback-copy preservation
Static grep confirmed:
- `useMemo(() => synthesizeIdealUser` in `ideal-user.tsx`: **1** (exact match for directive-mandated shape)
- `useMemo(() => synthesizeVersionOne` in `version-one.tsx`: **1** (exact match)
- `"A live draft of the person you are building for will begin to..."` in `ideal-user.tsx`: present at line 72 (verbatim preservation confirmed)
- `"A grounded version-one statement will begin to take shape here."` in `version-one.tsx`: present at line 66 (verbatim preservation confirmed)

The specific `useMemo(() => synthesize*)` pattern the `docs/v1-1-0-completion-guidance.md` Implementation Pattern mandates is present exactly once in each file. Empty-state fallback copy preserved verbatim in both files (no silent UI-copy drift — SCL Rule 5).

**Pass.** V5c provides the precise check that V5a's presence-level phrasing conflated with literal count.

### V6 — Non-scope integrity
All non-scope files confirmed untouched by this pass:
- `synthesizers.ts`, `structural-primitives.ts`, `reflection.tsx`, `transformation.tsx`, `blueprint.tsx`, `page.tsx`, and all other flow screens — no v1.0.23 modifications
- Six composing contract texts + terminal directive — no v1.0.23 modifications
- `docs/system-state/` — no new v1.0.23 entry; v1.0.20 remains authoritative
- External harness — no v1.0.23 modifications
- `.claude/CLAUDE.md` — no v1.0.23 modifications

**Pass.**

### Disk truth
- `app/session/flow/ideal-user.tsx` — modified (3 edits: import addition, useMemo computation, preview render)
- `app/session/flow/version-one.tsx` — modified (3 edits: parallel to ideal-user)
- `docs/reports/v1.0.23-preview-parity.md` — written (this file)
- `visionair_project_state.md` + `MEMORY.md` — pointers refreshed
- No other file touched

---

## Re-Entry Validation (per Prompt Artifact Contract §"Re-Entry Validation Rule")

1. **Compare execution against the re-sealed v1.0.23 artifact.** All 11 Execution Instructions executed in order. Six code edits (1a/1b/1c + 2a/2b/2c) applied as specified. T1 (V5a grep precision) honored in execution even though the spec phrasing itself was imprecise — my honest V5a report acknowledges this. T2 (V5b renamed "structural shape smoke test") executed as named — script output confirms shape compliance. T3 (V5c fallback-copy preservation) verified — both empty-state strings preserved verbatim. T4 (purpose wording) is a textual refinement; execution doesn't touch it. **No execution deviations.** One specification-accuracy gap surfaced (V5a literal count spec) — flagged honestly in §Validation.

2. **Verify scope discipline.** Two repo files modified; one report file created; two memory files updated. All §"Non-Scope" files confirmed untouched. **Scope held.**

3. **Verify adherence to all seven governance layers.**
   - Report Contract: nine required sections present; truth rule honored (V5a spec-gap disclosed honestly rather than papered over); filename convention followed ✓
   - SCL: Rule 5 directly engaged — preview now shows the synthesized form, eliminating "blueprint changed what I said" voice preservation gap on two surfaces; Rule 1 not engaged (synthesizer translation was already happening; this pass just makes it visible earlier) ✓
   - HLMC: Required Output Block present in §"Readiness / Next Move" below ✓
   - Prompt Artifact Contract: re-sealed artifact authorized ("execute v1.0.23"); human gate observed; Re-Entry Validation applied ✓
   - CASS: no override this pass; v1.0.20 state preserved; no §§1–4 triggered ✓
   - ARRSC: Phase 2 Claude-side memo-pass performed before authorization; memo-pass verdict was Pass; Phase 3 accept (user said "execute"); Phase 4 already in place via re-seal; Phase 5 authorization granted; execution completed ✓
   - `docs/v1-1-0-completion-guidance.md` terminal directive: four Implementation Pattern constraints all present (useMemo + structured bullets + blueprint SectionCard parity via `text-base leading-7 text-black/80` + UI-only); V5c directive-compliance check returned exact expected count ✓

4. **Validate outcomes match actual behavior.** Every V1–V6 claim traceable to specific observed tool output. V1 from `npx tsc --noEmit` empty output. V2 from `diff` exit 0. V4 from `diff` exit 0. V5a from grep counts (actually observed, flagged as spec-imprecise but outcome confirms edit landed). V5b from Node script exit 0 + observed structural checks. V5c from grep counts (exact match). V6 from git status review.

5. **Confirm no fabricated claims.** The V5a spec-accuracy note is exactly the kind of honest disclosure the Report Truth Rule mandates — counts are 2 for import+usage anchors, not 1, and the spec phrasing conflated import with usage. V5a still passes by intent; the binding directive-compliance gate is V5c, which passed exactly.

6. **Determine if the next move remains correct.** v1.0.20 roadmap and `docs/v1-1-0-completion-guidance.md` both name v1.0.24 bug_005 (capability weighting + calibration rerun) as the final bucket-2 pass and the v1.1.0-gating item. Three of four bucket-2 bugs now closed. **v1.0.24 confirmed as next move.**

**Re-Entry Validation passes.** Report approved to write. (This report IS the write.)

---

## Readiness / Next Move

### Current System State

**Working:**
- Seven-layer governance stack all respected; fourth consecutive successful code-execution pass (v1.0.21 re-seal 2, v1.0.22, v1.0.23)
- **Three of four bucket-2 bugs closed** (bug_011, bug_014, bug_006); final one remains (bug_005)
- Edit-screen previews now render synthesized bullets matching blueprint rendering — user sees the structured interpretation in context before committing to the blueprint
- Harness byte-identical to v1.0.18 baseline (no synthesizer touched since v1.0.18)
- `docs/system-state/v1.0.20-*` remains authoritative
- `docs/v1-1-0-completion-guidance.md` terminal directive operative and honored

**Not working / weak:**
- bug_005 (v1.0.24) remaining — capability per-answer weighting + calibration rerun; the hardest bucket-2 item, requires four directive-mandated weighting signals (repetition, emphasis language, specificity, domain keywords), calibration rerun against v1.0.18 baseline
- v1.0.16 pattern-quality polish items (P1 blocker vocabulary, context-regex precision) — deferred past bucket-2
- Regex-conjunction polish from v1.0.21 re-seal — deferred
- `synthesizeReflection` module-boundary exception still open
- V5a specification-phrasing gap observed in this pass — consider for future memo-pass discipline (anchor-counting spec should distinguish import from usage positions)

**Bottleneck:**
v1.0.24 is the final gate to v1.1.0. It is significantly more complex than v1.0.21/v1.0.22/v1.0.23 because:
- It changes `synthesizeCapability` substantively (per-answer weighting)
- It triggers a calibration rerun — output diffs EXPECTED, not forbidden
- Output-Diff Governance (Additive/Reordered/Substituted) WILL be engaged and must be explicitly classified per the directive

This is the first bucket-2 pass that will not produce byte-identical harness output.

### Highest-Leverage Next Move

**Execute v1.0.24 — bug_005 capability weighting + calibration rerun.** Final bucket-2 fix. Gates v1.1.0 declaration per `docs/v1-1-0-completion-guidance.md` readiness condition §1.

Per the terminal directive, v1.0.24 MUST:
- Introduce the four weighting signals: repetition, emphasis language, specificity, domain keywords
- Apply weighted scoring before theme selection; preserve deterministic structure
- Run full evaluation harness and compare against v1.0.18 baseline
- Measure improvement in Structural Cohesion + Output Fidelity
- Classify all output diffs as Additive / Reordered / Substituted
- No silent regressions; all diffs traceable to weighting logic

### Exact Next Step

Sealed-artifact loop per ARRSC:

1. User invokes **"Create sealed execution artifact for v1.0.24 (bug_005 capability weighting + calibration rerun)"**
2. Sealed artifact must specify: (a) operationalization of each of the four weighting signals — how each is detected in the 5 capability answers; (b) how weights combine into per-answer scores; (c) how per-answer scores flow into theme-detection scoring; (d) baseline harness capture step (re-use `v1018-harness.out`); (e) diff classification procedure; (f) success threshold for calibration improvement
3. Phase 2 memo-pass — must verify the four weighting signals all have concrete detection logic (not just vocabulary placeholders); Layer 3 Output-Diff Governance is now engaged (byte-identity is NOT the expected outcome — must have classification scheme)
4. Phase 3/4/5 proceed; execution on authorization; V5 + V6 classification of diffs; report + memory

### Why This Move

- Final bucket-2 item; gates v1.1.0 declaration
- Directive-mandated with four specific signals — design is constrained, reducing risk of memo-pass miss
- First pass to exercise Output-Diff Governance in full (Additive / Reordered / Substituted classification) — validates that governance rule empirically
- After v1.0.24, all six v1.1.0 readiness conditions can be evaluated

### What Not To Do Yet

1. **Skip the sealed-artifact + memo-pass loop for v1.0.24.** Mandatory per v1.0.19 ARRSC. Given v1.0.24's complexity (changing synthesizer logic + expected diffs), memo-pass discipline is more load-bearing here than anywhere else.
2. **Bypass the calibration rerun.** The terminal directive explicitly mandates it. v1.0.24 cannot be signed off without "compare against v1.0.18 baseline" being executed and reported.
3. **Combine bug_005 with the deferred polish items.** Scope discipline. Keep v1.0.24 bounded to the capability weighting surface only.
4. **Promote V5a spec-accuracy observation to CLAUDE.md.** One data point (this pass). Needs Recurring + Structural + Actionable per Instruction Assimilation Layer. Note it here, watch for pattern in v1.0.24 memo-pass.
5. **Retrofit prior reports.** Not rewriting history.

---

## Final Statement

bug_006 closed. Edit-screen previews on Ideal User and Version One now render the same synthesized bullet form rendered on the blueprint. SCL Rule 5 trust gap eliminated on two surfaces. Three of four bucket-2 bugs closed under the seven-layer governance stack (six composing contracts + terminal directive).

V5a surfaced a specification-phrasing gap honestly — import+usage anchors appear twice, not once, which the spec conflated. Implementation is correct; V5c's precise directive-compliance check confirms the exact mandated pattern. Flagged for future memo-pass discipline without promoting to CLAUDE.md yet.

v1.0.24 bug_005 is cleared as the final bucket-2 pass and v1.1.0 gate.
