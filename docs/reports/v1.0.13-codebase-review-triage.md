# Codebase Review Triage

## Report Version
v1.0.13

## Purpose
Process the findings from the external multi-agent codebase review (`/ultrareview 1` against PR #1, `REVIEW_FRAMING.md` as controlling brief) into the three roadmap-relative buckets the brief asks for, verify each finding against actual repo state, select the single highest-leverage next move, and re-sequence the post-v1.0.12 roadmap accordingly.

## Trigger
Completion of the cloud-run `/ultrareview` audit of `master` at `f3849e2` against `REVIEW_FRAMING.md`. Seven findings returned. User handoff authorized triage + roadmap re-sequencing as the next governed pass.

## State Observed (pre-triage)

Repo state at `master` `f3849e2`:
- Four synthesizers active (Reflection, Transformation, `synthesizeCapability`, `synthesizeVersionOne`); three inline in `app/session/page.tsx`
- `blueprintData` at `page.tsx:546-559` composes seven fields; three route synthesizer `string[]` output through `.join(' ')` before storing as `string`
- `BlueprintData` at `blueprint.tsx:6-18` types those three fields as `string`; `SectionCard` at `blueprint.tsx:26-43` renders via a single `<p>`
- `pathForward` already ships structured `{immediate, nearTerm, later}` rendering at `blueprint.tsx:95-128` — existing in-repo precedent for per-field structured rendering
- Planned v1.0.13 per v1.0.12 report: `synthesizeIdealUser` inline, replacing the `structuralizeBefore` misroute on the idealUser field

Seven findings returned by the review (all verified against disk):

1. **bug_004** — `themePatterns` (`reflection.tsx:41-50`, 8 themes, narrower regex) and `capabilityThemePatterns` (`page.tsx:101-110`, same 8 themes, strict-superset regex in 6 of 8) are parallel vocabularies with identical-shape scorers. `detectCapabilityTargets` already reaches across synthesizer boundaries at `page.tsx:445`.
2. **bug_011** — `detectVersionOneScope` (`page.tsx:358-396`) exclusion regex at line 361 captures any 1–2 word phrase after `no`/`without` and filters only 6 tokens. Common English discourse markers (`no doubt`, `no problem`, `no time`, `no way`, `no good reason`) pass the filter and render as user-attributed scope choices in blueprint Section 6. The exclusion branch returns at line 369-372 before any other scope detector runs.
3. **bug_014** — `state.reflection` (`page.tsx:49`, initialized `page.tsx:67`, written `page.tsx:591`) is never read anywhere. The Reflection screen's prominent refine/recalibrate CTAs (`reflection.tsx:385-399`) plumb the user's typed correction into this field; nothing downstream consumes it.
4. **merged_bug_001** — `idealUser` is routed through `structuralizeBefore` (`page.tsx:549-551`), whose regex table (`transformation.tsx:22-47`) is exclusively emotional/absence vocabulary with zero noun-phrase detectors for person descriptions. Same defect class v1.0.12 shipped to fix on `versionOne`. The comment block at `page.tsx:536-545` also contains a stale claim on line 542 (`versionOne: structuralized via the "after" translator`) that no longer matches runtime.
5. **bug_006** — `ideal-user.tsx:59-60` and `version-one.tsx:53-54` render the raw textarea `{value}` in live-preview cards, while the blueprint replaces those strings with synthesizer output. Transformation screen (`transformation.tsx:177-209`) already renders the synthesizer output as bullets on the edit screen — the correct pattern exists in-repo but was not extended.
6. **bug_005** — `synthesizeCapability` (`page.tsx:195-196`) collapses the 5 semantically distinct capability answers via `answers.filter(Boolean).join(' ')` before any detection. Q5 preference content (weakest capability evidence) can outweigh Q2 external-demand content (SCL-Priority-1 layer) by regex match count.
7. **bug_002** — `blueprintData` flattens `synthesizeCapability`/`structuralizeBefore`/`synthesizeVersionOne` `string[]` output via `.join(' ')` at `page.tsx:547,550,557`. `BlueprintData.capability/idealUser/versionOne` are typed `string`; `SectionCard` renders via a single `<p>`. The same synthesizer output renders as `<ul><li>` bullets on the transformation screen — one surface shows bullets, the other shows paragraphs.

## Action Taken

### 1. Verified all seven findings against actual disk state
Each finding was checked against the cited file/line. All seven are real; no false positives. No contradictions with `REVIEW_FRAMING.md`'s known-deliberate carve-outs (`pathForward` raw passthrough, step-machine order, temporary `console.log`, inline-synthesizer pattern).

### 2. Triaged findings into the three roadmap-relative buckets per `REVIEW_FRAMING.md`

#### Bucket 1 — Must address before the next synthesizer lands

These are the findings that make the planned `synthesizeIdealUser` land cleanly rather than locking in additional drift.

- **bug_002 (blueprint flattens `string[]` via `.join(' ')`).** Source-of-truth contract issue at the final artifact surface. Adding a fourth synthesizer to the current flattened contract bakes in another `.join(' ')` call on the data-flow path the review flags as most expensive to undo. Fix now → new synthesizer returns bullets into a bullets-typed contract with no additional rework.
- **merged_bug_001's comment-drift half.** The stale claim at `page.tsx:542` will mislead the v1.0.14 contract-widening pass and any future extraction pass that reads the comment block as the reconciliation map. Fix when the same lines are touched.

#### Bucket 2 — Must address before the v1.1.0 user-testing milestone

User-visible artifact-integrity issues that will surface when real users see the blueprint or interact with the full flow.

- **bug_011 (`detectVersionOneScope` over-matches `no X`).** Blueprint Section 6 fabricates user-attributed scope from common English discourse markers. Direct user-trust hit on the most concrete blueprint card. Denylist expansion is low-risk (no calibration rerun needed for that form of fix).
- **bug_014 (`state.reflection` write-only).** The refine/recalibrate affordance is visible, prominent, and nonfunctional — the exact class of broken user-signal path that erodes artifact trust. Product decision required: consume downstream, feed into a synthesizer, or change the copy and reserve the field.
- **bug_006 (live preview shows raw, blueprint shows synthesized).** Users see their own prose on the edit screen and substituted synthesizer text on the blueprint for `idealUser` and `versionOne`. Existing in-repo precedent (`transformation.tsx:177-209`) already shows the correct pattern. Small plumbing fix.
- **bug_005 (`synthesizeCapability` flattens 5 answers).** Per-answer provenance (externally-observable Q2/Q4 vs preference Q5) is discarded before detection runs. Fixing requires calibration rerun against the 3.36/5 baseline, so plan accordingly — but before real users see the capability bullet as their dominant opening frame.
- **bug_004 (duplicated theme primitives).** User-visible as subtle framing drift between Reflection and Capability surfaces on identical input. Can be absorbed into the planned v1.0.14/v1.0.15 extraction pass per the review's own second-best recommendation, so do not hold up the immediate move.

#### Bucket 3 — Safe to defer past v1.1.0

None. All seven findings affect either blueprint-artifact integrity, user-facing surface asymmetry, or architectural cost that compounds at the next synthesizer. The review is candid that nothing here is optional long-term; the buckets are a sequencing question, not a keep/drop question.

### 3. Re-sequenced the post-v1.0.12 roadmap

Prior plan per v1.0.12 report:
- v1.0.13 = `synthesizeIdealUser`
- v1.0.14 = extraction of inline synthesizers to shared module

Revised plan after triage:
- **v1.0.13** (this report) = codebase review triage, no code change
- **v1.0.14** = blueprint data contract widening (the highest-leverage move named below)
- **v1.0.15** = `synthesizeIdealUser` (the originally-planned v1.0.13 move, now sequenced second so it lands into the widened contract)
- **v1.0.16** = extraction + primitives consolidation (absorbs bug_004's cross-file theme-vocabulary reach)
- **v1.0.17+** = bucket-2 fixes (bug_011 denylist, bug_014 product decision, bug_006 preview parity, bug_005 per-answer weighting + calibration rerun), sequenced before the v1.1.0 user-testing milestone

## Result
- Seven findings verified, bucketed, and sequenced against the project's own roadmap axis per `REVIEW_FRAMING.md`'s instruction
- Post-v1.0.12 roadmap re-sequenced so the next code pass is a data-flow fix (contract widening) rather than a logic addition (new synthesizer on top of a flawed contract)
- Stale roadmap assumption from v1.0.12 ("v1.0.13 = synthesizeIdealUser") explicitly superseded
- Three bucket-2 findings flagged with explicit fix-shape notes so the sequencing after v1.0.15 does not require rediscovery
- All known-deliberate carve-outs in `REVIEW_FRAMING.md` left intact (inline pattern, raw `pathForward`, step-machine order, temporary `console.log`)

## Validation
- Each finding was re-checked against the cited file/line before triage. All claims verified; no finding was over-stated or hallucinated.
- Triage respects the `REVIEW_FRAMING.md` definition of "expensive to undo" (code rewrite cost + doctrinal erosion cost + LLM migration cost + user-trust cost) and uses the brief's roadmap-relative bucket axis, not fuzzy urgency language.
- Re-sequencing is consistent with the Highest-Leverage Move Contract's prioritization rules: system-level over surface (bug_002 over bug_011), data-flow over logic (bug_002 contract over a new synthesizer), integration over isolation (contract widening affects all four synthesizers including the one not yet written).
- No runtime state changed. No governance file changed. This is a decision pass, not an execution pass.

---

## Readiness / Next Move

### Current System State

**Working:**
- All four existing synthesizers produce their intended output shape (reflection, transformation, capability, versionOne) as validated by the 10-persona harness at 3.36/5 baseline
- Three governance contracts compose and continue to govern every reportable pass
- External `/ultrareview` audit completed, findings returned, and triage integrated

**Not working / weak:**
- **Blueprint data contract flattens deliberately-structured synthesizer bullets into paragraphs at the final artifact surface** — the same synthesizer output renders as `<ul><li>` bullets on the transformation edit screen but as a single `<p>` on the blueprint. The planned v1.0.13 would add a fourth `.join(' ')` call to this path if executed as originally spec'd.
- `idealUser` still hits `structuralizeBefore` fallback for realistic person-description inputs (unchanged since v1.0.9; was scheduled for v1.0.13 fix)
- Five further artifact-integrity issues in bucket 2 are now explicitly logged for pre-v1.1.0 sequencing

**Bottleneck:**
The blueprint is the primary artifact the user takes away from the session. Three of its seven sections currently receive flattened paragraphs where the synthesizers produced structured bullets. Landing `synthesizeIdealUser` on top of this flattening — as originally planned — would make a fourth section the same shape and push the contract widening behind an extraction pass that inherits the problem. The source-of-truth surface is the `BlueprintData` type and the `SectionCard` renderer, not any individual synthesizer.

### Highest-Leverage Next Move

**Widen `BlueprintData.capability`, `BlueprintData.idealUser`, and `BlueprintData.versionOne` from `string` to `string[]`, remove the three `.join(' ')` calls in `blueprintData` composition, and update `SectionCard` to render `string[]` as a `<ul><li>` bullet list matching the pattern already in place on the transformation edit screen.**

### Exact Next Step

Single pass, titled **v1.0.14 — Blueprint Data Contract Widening**. Two files touched, no synthesizer logic changes, no new features.

1. **`app/session/flow/blueprint.tsx`**
   - Change `BlueprintData.capability`, `BlueprintData.idealUser`, `BlueprintData.versionOne` from `string` to `string[]`
   - Split `SectionCard` into two variants (or accept `string | string[]` with internal branching): bullet-list form renders `<ul className="space-y-2"><li>• {b}</li></ul>` matching `transformation.tsx:177-204`; paragraph form remains for `problemSpace`, `transformation`, `opportunityForm`
   - Keep the `Still taking shape.` empty-state fallback; apply to empty-array case for the widened fields

2. **`app/session/page.tsx`**
   - Remove `.join(' ')` from `blueprintData.capability` (line 547), `blueprintData.idealUser` (lines 549-551), and `blueprintData.versionOne` (line 557); store the `string[]` directly
   - Update the comment block at lines 536-545 to match runtime: `idealUser` entry should say "still routed through `structuralizeBefore` pending v1.0.15 `synthesizeIdealUser`"; `versionOne` entry (line 542) should say "synthesized via `synthesizeVersionOne` (v1.0.12)" — correcting the stale `after translator` claim
   - Do not change any synthesizer logic

3. **Validation**
   - Recompile (`✓ Compiled` expected)
   - Re-run the 10-persona harness; confirm blueprint sections 1/3/6 now render as bullet lists; confirm sections 2/4/5/7 unchanged; confirm empty-state rendering unchanged
   - No calibration shift expected — the content is identical; only the render shape changes

4. **Report v1.0.14** following the Report Contract + Highest-Leverage Move Contract, with before/after screenshots or text-dump of at least two blueprint renders.

### Why This Move

- **Impact relative to alternatives**: widens the source-of-truth contract that three current synthesizers (and the not-yet-written fourth) all feed into. Fixing it now means `synthesizeIdealUser` at v1.0.15 returns bullets directly into a bullets-typed contract with zero additional plumbing. Fixing it later means touching every synthesizer composition line twice and re-running calibration for a no-op content change.
- **What it unlocks**: per SCL Rule 5 ("Preserve human truth, but compress it into structured language") and the external `REVIEW_FRAMING.md` brief's user-trust axis, the blueprint's deliberate structure becomes visible on the final artifact rather than being erased at the render boundary. The asymmetry where transformation shows bullets but capability shows a paragraph disappears.
- **Why sequenced now**: per Highest-Leverage Move Contract Rule 4 ("data flow fixes over logic tweaks — if output is wrong, check source-of-truth first"), the blueprint data contract is the source-of-truth for every downstream render. Per Rule 3 ("integration over isolation"), it is the one change that affects all four synthesizers at once. Per Rule 2 ("system-level over surface fixes"), it is architectural, not a surface polish. All three rules point to the same move.
- **Why not `synthesizeIdealUser` first**: executing the originally-planned v1.0.13 would add a fourth `.join(' ')` to the very contract that the review flags as most expensive to undo. The contract widening is cheaper to do before `synthesizeIdealUser` lands, not after. Sequencing reversed for that reason.

### What Not To Do Yet

1. **`synthesizeIdealUser` (originally planned as v1.0.13, now v1.0.15)** — Lower leverage *right now* because it would land into a still-flattened contract, requiring either an immediate rework of the one-line blueprint composition or accepting another `.join(' ')`. Do this immediately after v1.0.14; the spec in the v1.0.12 report is unchanged.

2. **Primitives consolidation / synthesizer extraction (v1.0.16)** — Lower leverage right now because the review's own second-best option is to absorb it into the extraction pass after the fourth synthesizer lands. Still on the roadmap; do not skip. Consolidating theme/target vocabulary from `reflection.tsx` + `page.tsx` + the planned `synthesizeIdealUser` blocker-regex is a natural part of that pass.

3. **Bucket 2 user-facing fixes (bug_011 scope denylist, bug_014 reflection state, bug_006 preview parity, bug_005 per-answer weighting)** — Lower leverage right now because none is a blocker on v1.0.14 or v1.0.15. All must land before v1.1.0 user testing; sequence them immediately after v1.0.16 extraction, probably as v1.0.17 through v1.0.19. Do not batch them into v1.0.14 — scope discipline matters and the 10-persona calibration baseline gets perturbed by bug_005.

4. **Register the three contracts in `.claude/CLAUDE.md`** — Same reasoning as every prior report: documentation cleanup, not runtime alignment. Per Rule 5 ("runtime behavior alignment over documentation updates"), defer until a runtime pass benefits from explicit cross-reference.
