// AUTO-GENERATED — do not edit by hand.
// Source of truth: lib/build-mode/presets/*
// Regenerate after editing a preset: node lib/build-mode/_gen-presets.cjs
/* eslint-disable */
export const PRESET_FILES: Record<string, string> = {
  "security.md": "<!-- [preset] VALIDATED DEFAULT for the Next.js/TS/Vercel house stack — verify against THIS app; flag any mismatch as a gap in docs/context/07-known-gaps.md. Authority: see citations inline. -->\n\n# 06 — Security Baseline\n\n> Authority: OWASP Top 10:2025 (A01 Broken Access Control, A02 Cryptographic/Secrets, A03 Software Supply Chain Failures, A05 Security Misconfiguration); OWASP Secure Headers Project; OWASP ASVS v4; 12-Factor App §III Secrets.\n\n---\n\n## HTTP Security Headers\n\nApply in `next.config.mjs` → `headers()` for all routes:\n\n| Header | Safe uniform value | Notes |\n|---|---|---|\n| `X-Content-Type-Options` | `nosniff` | Prevents MIME sniffing |\n| `X-Frame-Options` | `DENY` | Stops clickjacking; also use CSP `frame-ancestors 'none'` |\n| `Content-Security-Policy` | `frame-ancestors 'none'` ONLY | See GAP below for full source allowlist |\n| `Referrer-Policy` | `strict-origin-when-cross-origin` | Safe default for most apps |\n| `Permissions-Policy` | `camera=(), microphone=(), geolocation=()` | Restrict unused browser APIs |\n| `Strict-Transport-Security` | `max-age=63072000; includeSubDomains` | 2-year HSTS **without** `preload` |\n| `X-Powered-By` | *(strip — set `poweredByHeader: false` in next.config)* | Hides stack fingerprint |\n\n### GAP: CSP source allowlist\n**Do NOT use a generic or placeholder CSP value.** A CSP must enumerate every legitimate content source for THIS app (scripts, styles, images, fonts, connect-src for APIs/analytics/etc.). A permissive generic CSP (`'unsafe-inline' 'unsafe-eval'`) negates protection; a strict nonce-CSP also forces dynamic rendering which disables Vercel ISR/PPR caching (documented Next.js tradeoff). **Action:** elicit the full source list and author the per-app CSP. Track in `docs/context/07-known-gaps.md` until resolved.\n\n### GAP: HSTS preload\n`preload` submits the domain to browser preload lists — a one-way permanent commitment. Do NOT add `preload` until you have verified the domain is production-stable, all subdomains support HTTPS, and you understand the removal process. Track in known-gaps.\n\n### GAP: Rate limiting\nIn-memory rate limiting does not work on Vercel's serverless/edge runtime across invocations and regions — it gives false confidence with no real protection. Use an external store: **Upstash Redis KV** (recommended for Vercel) or **Vercel WAF** (enterprise). Elicit per-route thresholds. Track in known-gaps.\n\n---\n\n## Secrets Management\n\n- **Server-only secrets** — all tokens, API keys, and credentials belong in environment variables loaded server-side. Never in source code.\n- **No secrets in `NEXT_PUBLIC_*`** — any variable prefixed `NEXT_PUBLIC_` is bundled into the client JavaScript and visible to all users. This includes tokens, internal API URLs, and analytics keys that carry write permissions.\n- **`.env.example`** — commit a `.env.example` listing every required variable name (no values) as the authoritative onboarding reference. Never commit `.env` or `.env.local`.\n- **Secret scanning** — gitleaks runs in CI (see `ci.yml`) and as a pre-commit hook. GitHub push-protection MUST be enabled on the repo. Rotate any secret that was ever committed.\n\n---\n\n## AuthZ / App Security\n\n### Data-layer authz scoping\nEvery database query MUST be scoped to the authenticated principal. Never load records by ID alone — always include the owning user/tenant identifier as a filter. A missing authz scope on a single query is an A01 Broken Access Control finding.\n\n```ts\n// CORRECT — scope every query to the caller\nconst post = await db.post.findUnique({ where: { id, authorId: session.userId } });\n\n// WRONG — any authenticated user can access any record\nconst post = await db.post.findUnique({ where: { id } });\n```\n\n### CSRF\nCookie/session mutations and Next.js Server Actions require CSRF protection:\n- Verify the `Origin` / `Referer` header on all state-changing server-side handlers.\n- For Server Actions (`\"use server\"`): Next.js 14+ validates same-origin automatically for form-based invocations, but explicitly check for cross-origin fetch callers.\n- Use `SameSite=Strict` or `SameSite=Lax` on session cookies.\n\n### SSRF (Server-Side Request Forgery)\nAny server-side `fetch()` or HTTP client call using a URL derived from user input is an SSRF risk:\n- Validate and allowlist the scheme (`https` only) and destination host before fetching.\n- Never forward raw user-supplied URLs to external services.\n\n### Webhook signature verification\nAny endpoint receiving webhook payloads MUST verify the provider's HMAC signature before processing:\n```ts\nconst sig = req.headers.get(\"x-webhook-signature\");\nconst expected = hmac(secret, rawBody);\nif (!timingSafeEqual(sig, expected)) return new Response(\"Unauthorized\", { status: 401 });\n```\nReject the request before touching the payload if the signature is absent or invalid.\n\n---\n\n## Supply-Chain / CI\n\n- Dependabot (grouped, weekly) — see `.github/dependabot.yml`.\n- SHA-pinned GitHub Actions — see `.github/workflows/ci.yml`.\n- `npm ci` (not `npm install`) in all CI runs — enforces lockfile.\n- Trivy vulnerability scanning and SARIF upload — **conditional**; see `ci.yml` comments. SARIF upload requires GitHub code-scanning, which is not available on private repos under GitHub Free/Pro. Trivy is also lower-signal for non-containerized JS apps. Enable when applicable.\n\n---\n\n## Prompt Injection (if this app uses an LLM)\n\n**Only if this app calls an LLM:**\n- Treat all LLM output as untrusted before rendering or acting on it.\n- Never include user-controlled content in the system prompt without sanitization.\n- Maintain an explicit tool/function allowlist; never let the model call arbitrary functions.\n- Require human-in-the-loop for irreversible side effects (sends, writes, deletes).\n\nTrack whether this app has an LLM in `docs/context/00-identity.md`. If it does, promote this section from advisory to required.\n\n---\n\n## GAP Tracker (seed)\n\nAdd app-specific security items to `docs/context/07-known-gaps.md`:\n\n- [ ] CSP full source allowlist (required before production)\n- [ ] HSTS preload decision (required before domain submission)\n- [ ] Rate-limit strategy and thresholds (required for any authenticated endpoint)\n- [ ] App threat model (STRIDE or equivalent)\n- [ ] Auth model + tenancy pattern (drives authz scoping implementation)\n- [ ] Trivy/SARIF enablement decision (depends on repo visibility + containerization)\n",
  "architecture.md": "<!-- [preset] VALIDATED DEFAULT for the Next.js/TS/Vercel house stack — verify against THIS app; flag any mismatch as a gap in docs/context/07-known-gaps.md. Authority: see citations inline. -->\n\n# 05 — Architecture Baseline\n\n> Authority: 12-Factor App (Heroku, Wiggins/Beyer); Michael Nygard — \"Documenting Architecture Decisions\" (2011); Next.js Project Structure docs; Fowler ADR catalog.\n\n---\n\n## Principles\n\n1. **One codebase, many deploys** — a single version-controlled repo per app. No branching per environment. Config in environment variables (12-Factor III).\n2. **Explicit dependencies** — all dependencies declared in `package.json`, reproducible via `npm ci`. No ambient globals.\n3. **Config in the environment** — nothing that varies between deploys belongs in code. See `SETUP.md` and `.env.example`.\n4. **Stateless processes** — server processes do not hold in-memory state between requests. State lives in the data layer (DB, cache, queue).\n5. **Decisions are documented** — any architectural choice with consequences that are hard to reverse gets an ADR. Short ADRs are better than no ADRs.\n\n---\n\n## Next.js Conventions (App Router)\n\n| Layer | Rule |\n|---|---|\n| `app/` | Routing only — layouts, pages, loading/error shells. No business logic here. |\n| `app/api/` | API routes. Thin handlers — parse request, delegate to domain layer, return response. |\n| Server Components | Default. Fetch data here. Never pass sensitive data as props to Client Components. |\n| Client Components | Opt-in (`\"use client\"`). UI interactivity only. No direct DB access. |\n| Validation | At the boundary — API routes and Server Actions validate all inputs (Zod recommended). Never trust client-sent data. |\n| Server Actions | `\"use server\"` directives. Validate inputs, scope all DB queries to the authenticated user (see `06-security.md`). |\n\n### GAP: Folder taxonomy\nThis preset does NOT prescribe a fixed folder structure (e.g. `services/`, `domain/`, `lib/`). A fixed skeleton fights the App Router's colocation model and over-abstracts v1 apps. **Elicit the actual module boundaries** from `docs/context/03-spec.md` and decide the folder layout as a tracked ADR. Track in `docs/context/07-known-gaps.md` until resolved.\n\n---\n\n## ADR Template (Nygard)\n\nCreate one file per decision under `docs/decisions/` (e.g. `docs/decisions/001-database-choice.md`):\n\n```markdown\n# ADR-NNN — [Short decision title]\n\n**Status:** [Proposed | Accepted | Superseded by ADR-NNN | Deprecated]\n**Date:** YYYY-MM-DD\n**Deciders:** [names or roles]\n\n## Context\n\n[What is the issue motivating this decision? What forces are at play?]\n\n## Decision\n\n[The change we are making, stated in active voice: \"We will...\"]\n\n## Consequences\n\n**Positive:**\n- [benefit 1]\n\n**Negative / trade-offs:**\n- [cost or constraint 1]\n\n**Neutral:**\n- [neutral consequence]\n```\n\n---\n\n## Supply-Chain Integrity\n\n- Pin all production dependencies to exact versions in `package-lock.json` (committed).\n- Use `npm ci` (not `npm install`) in CI and deployment pipelines.\n- Declare `engines` in `package.json` to pin the Node.js version.\n- Dependabot (grouped updates, weekly) keeps dependencies current — see `.github/dependabot.yml`.\n",
  "testing.md": "<!-- [preset] VALIDATED DEFAULT for the Next.js/TS/Vercel house stack — verify against THIS app; flag any mismatch as a gap in docs/context/07-known-gaps.md. Authority: see citations inline. -->\n\n# Testing Baseline\n\n> Authority: Kent C. Dodds — \"The Testing Trophy\" (2018); Martin Fowler — \"Test Pyramid\" + \"TestCoverage\" (2012/2023); Fowler — \"On the Diverse And Fantastical Shapes of Testing\".\n\n---\n\n## Testing Trophy (integration-weighted)\n\nFor a Next.js/TypeScript app, use the **Testing Trophy** shape rather than the classic pyramid:\n\n```\n        /\\\n       /e2e\\          (thin — conditional on UI surface)\n      /------\\\n     /integr- \\       <-- MOST TESTS LIVE HERE\n    / ation    \\\n   /------------\\\n  / unit (pure)  \\    (for pure functions, utilities, domain logic)\n /----------------\\\n/ static (TS+lint) \\   (free — always on)\n```\n\n**Why integration-weighted?** Next.js Server Components, API routes, and Server Actions are best tested at the integration level (real request/response, in-memory DB or MSW mocks). Pure unit tests miss the framework contract; shallow component tests give false confidence. Integration tests are the best ROI for this stack.\n\n---\n\n## What to test first\n\n1. **AuthZ paths** — every endpoint and Server Action that touches data must have a test proving an unauthenticated or unauthorized caller gets a 401/403, not data.\n2. **Validation paths** — every input schema (Zod or equivalent) has a test for invalid input producing a useful error, not a 500.\n3. **The money path** — the core user flow that makes or breaks the app's value. If something here breaks, you know immediately.\n4. **Known-gaps and security controls** — any item in `07-known-gaps.md` that has a code fix should have a test that would have caught the gap.\n\n---\n\n## Coverage\n\n**Coverage is REPORT-ONLY — no hard threshold gate.** Reasons:\n- A hard coverage gate trains teams to write useless tests to hit the number rather than testing risk.\n- 80% coverage on unimportant code is worse than 60% coverage on the critical path.\n- The signal is the trend (is coverage growing?) and the gap (what important code is uncovered?), not a pass/fail binary.\n- Authority: Fowler \"TestCoverage\" — \"I would be suspicious of anything like 80% as a universal rule.\"\n\nConfigure Vitest to generate a coverage report (`v8` or `istanbul`) and commit the configuration — just do not fail the build on it.\n\n---\n\n## Tool Stack\n\n| Layer | Tool | Note |\n|---|---|---|\n| Static / type | TypeScript + ESLint | Zero config overhead |\n| Unit + integration | Vitest + `@testing-library/react` | Fast, ESM-native |\n| API/route testing | Vitest + `node:http` or MSW | Integration level |\n| E2E | Playwright | **Conditional** — see GAP below |\n\n### GAP: E2E / Playwright\nE2E tests against a running browser are high-value for apps with complex UI flows and low-value for API-only apps. Playwright is also expensive to maintain. **Elicit the UI surface** from `docs/context/03-spec.md` before adding Playwright. Track in `docs/context/07-known-gaps.md`.\n\n---\n\n## TDD Loop (per feature)\n\n1. Write the failing test (describes desired behavior, not implementation).\n2. Run — confirm it fails for the right reason.\n3. Implement the minimum code to pass.\n4. Run — confirm it passes.\n5. Refactor (optional) — run again to confirm still green.\n6. Commit.\n\nAuthority: Beck \"Test-Driven Development by Example\" (2002).\n",
  "workflow.md": "<!-- [preset] VALIDATED DEFAULT for the Next.js/TS/Vercel house stack — verify against THIS app; flag any mismatch as a gap in docs/context/07-known-gaps.md. Authority: see citations inline. -->\n\n# 08 — Workflow Baseline\n\n> Authority: Beck \"TDD by Example\" (2002); Fowler \"Is Design Dead?\" (2004); Fowler \"Continuous Delivery\" (Humble/Farley, 2010).\n\n---\n\n## The Durable Loop (works plugin-free)\n\nEvery feature or fix follows this sequence. Scale the rigor to the risk (see table below):\n\n```\nSPEC → PLAN → TDD → REVIEW → VERIFY → SHIP\n```\n\n| Phase | What happens | Artifact |\n|---|---|---|\n| **Spec** | Define the behavior, inputs/outputs, and non-negotiables. Confirm with the team. | `docs/context/03-spec.md` or ADR |\n| **Plan** | Break into ordered tasks. Identify unknowns. No code yet. | Numbered task list |\n| **TDD** | Write failing test → implement minimum → pass → refactor | Tests + implementation |\n| **Review** | Code review: correctness, security (authZ, validation, secrets), maintainability | PR comments |\n| **Verify** | Smoke test in a real environment. Check the actual behavior, not just test output | Manual or automated |\n| **Ship** | Merge → deploy → monitor | Deployment |\n\n---\n\n## Right-Sizing Rule\n\nScale rigor to app risk, not to process habit:\n\n| Signal | Lower rigor OK | Higher rigor required |\n|---|---|---|\n| Data sensitivity | Public/anonymous data | PII, financial, medical |\n| Mutation surface | Read-only or internal | User writes, sends, deletes |\n| Traffic scale | Low / known users | Public or high volume |\n| Reversibility | Easy to roll back | Hard to undo (emails sent, money moved) |\n\nA one-page internal tool does not need the same review depth as a public-facing payment flow. Document the risk level in `docs/context/01-non-negotiables.md` so the team has a shared calibration.\n\n---\n\n## Optional Accelerators (if installed)\n\nThe following `superpowers:*` skills can accelerate specific phases — **they are entirely optional** and assume the [Superpowers plugin](https://github.com/anthropics/claude-code-superpowers) is installed in Claude Code:\n\n- `superpowers:brainstorming` — before authoring a spec (explores intent + requirements)\n- `superpowers:writing-plans` — converts a spec into a numbered, testable plan\n- `superpowers:test-driven-development` — guides the TDD loop per task\n- `superpowers:requesting-code-review` — structured review at PR time\n- `superpowers:verification-before-completion` — final smoke-test checklist\n\n**Never assume these are present.** The durable loop above works without them.\n\n---\n\n## Known-Gaps Integration\n\nAny item that is unresolved, undecided, or contradictory goes into `docs/context/07-known-gaps.md` immediately — never silently deferred or assumed away. The engine's reconciliation pass surfaces preset↔app mismatches; human judgment resolves them.\n",
  "ci.yml": "# [preset] VALIDATED DEFAULT for the Next.js/TS/Vercel house stack — verify against THIS app; flag any mismatch as a gap in docs/context/07-known-gaps.md.\n# Authority: GitHub Actions secure-use guide; SLSA v1.0; OWASP A03:2025 Software Supply Chain Failures.\n\nname: CI\n\non:\n  pull_request:\n    branches: [main]\n  # NOTE: Never use pull_request_target with secrets — it runs in the context of the target\n  # branch and grants write access to PRs from forks. Use pull_request instead.\n\n# Least-privilege token: read-only by default; individual jobs/steps opt-in to write scopes.\npermissions:\n  contents: read\n\nconcurrency:\n  group: ci-${{ github.ref }}\n  cancel-in-progress: true\n\njobs:\n  build-and-test:\n    runs-on: ubuntu-latest\n\n    steps:\n      - name: Checkout\n        # actions/checkout v4.2.2\n        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683\n\n      - name: Setup Node\n        # actions/setup-node v4.4.0\n        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020\n        with:\n          node-version: \"20\"\n          cache: \"npm\"\n\n      - name: Install dependencies (lockfile-enforced)\n        run: npm ci\n\n      - name: Type check\n        run: npx tsc --noEmit\n\n      - name: Build\n        run: npm run build\n\n      - name: Test\n        run: npm test\n        # Coverage is REPORT-ONLY — no hard threshold.\n        # Failing a build on an arbitrary coverage number trains teams to ignore the signal.\n        # Rely on code review and Testing Trophy discipline for quality gates.\n\n      - name: npm audit (report-only)\n        run: npm audit --audit-level=none || true\n        # npm audit is HIGH NOISE / advisory. Do not fail the build on audit findings.\n        # Use Dependabot (see dependabot.yml) + Trivy for actionable supply-chain gating.\n\n  secret-scan:\n    runs-on: ubuntu-latest\n    steps:\n      - name: Checkout\n        # actions/checkout v4.2.2\n        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683\n        with:\n          fetch-depth: 0\n\n      - name: Gitleaks secret scan\n        # gitleaks/gitleaks-action v2.3.9\n        uses: gitleaks/gitleaks-action@ff98106e4c7b2bc287b24eaf42907196329070c7\n        env:\n          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}\n\n  # CONDITIONAL: Trivy vulnerability scan + SARIF upload.\n  # Requirements before enabling:\n  #   - GitHub code-scanning must be enabled (not available on private repos under Free/Pro tier).\n  #   - This job adds ~2 min and is lower-signal for non-containerized JS apps.\n  #   - Enable when: repo is public OR code-scanning is available on your plan.\n  #\n  # trivy-scan:\n  #   runs-on: ubuntu-latest\n  #   permissions:\n  #     contents: read\n  #     security-events: write   # needed to upload SARIF\n  #   steps:\n  #     - name: Checkout\n  #       # actions/checkout v4.2.2\n  #       uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683\n  #\n  #     - name: Run Trivy vulnerability scanner\n  #       # aquasecurity/trivy-action — pin SHA before enabling\n  #       uses: aquasecurity/trivy-action@<SHA>  # TODO: resolve SHA\n  #       with:\n  #         scan-type: \"fs\"\n  #         format: \"sarif\"\n  #         output: \"trivy-results.sarif\"\n  #\n  #     - name: Upload SARIF to code-scanning\n  #       uses: github/codeql-action/upload-sarif@<SHA>  # TODO: resolve SHA\n  #       with:\n  #         sarif_file: \"trivy-results.sarif\"\n",
  "dependabot.yml": "# [preset] VALIDATED DEFAULT for the Next.js/TS/Vercel house stack — verify against THIS app; flag any mismatch as a gap in docs/context/07-known-gaps.md.\n# Authority: GitHub Dependabot docs; OWASP A03:2025 Software Supply Chain Failures; SLSA v1.0.\n\nversion: 2\n\nupdates:\n  # npm dependencies — grouped to reduce PR noise\n  - package-ecosystem: \"npm\"\n    directory: \"/\"\n    schedule:\n      interval: \"weekly\"\n      day: \"monday\"\n      time: \"09:00\"\n      timezone: \"America/Los_Angeles\"\n    groups:\n      all-npm:\n        patterns:\n          - \"*\"\n    open-pull-requests-limit: 5\n    labels:\n      - \"dependencies\"\n\n  # GitHub Actions — grouped; keep actions current for security patches\n  - package-ecosystem: \"github-actions\"\n    directory: \"/\"\n    schedule:\n      interval: \"weekly\"\n      day: \"monday\"\n      time: \"09:00\"\n      timezone: \"America/Los_Angeles\"\n    groups:\n      all-github-actions:\n        patterns:\n          - \"*\"\n    open-pull-requests-limit: 5\n    labels:\n      - \"dependencies\"\n      - \"github-actions\"\n",
  "pre-commit-config.yaml": "# [preset] VALIDATED DEFAULT for the Next.js/TS/Vercel house stack — verify against THIS app; flag any mismatch as a gap in docs/context/07-known-gaps.md.\n# Authority: pre-commit.org; gitleaks docs; OWASP A03:2025 Supply Chain.\n#\n# NOTE — JS/TS projects: prefer Husky + lint-staged for local hooks.\n# The Python `pre-commit` framework (https://pre-commit.com) requires Python on every\n# developer machine. For a Node.js project, Husky integrates natively with npm lifecycle\n# scripts and does not require a separate runtime.\n#\n#   Husky setup (recommended alternative):\n#     npm install --save-dev husky lint-staged\n#     npx husky init\n#     # Add to .husky/pre-commit:\n#     #   npx lint-staged\n#     # Add to package.json:\n#     #   \"lint-staged\": { \"*.{ts,tsx}\": [\"eslint --fix\", \"tsc --noEmit\"] }\n#\n# This file is the FALLBACK if the team is already using pre-commit.com for other reasons.\n#\n# IMPORTANT: Local hooks are bypassable with `git commit --no-verify`.\n# Always back local hooks with CI (see .github/workflows/ci.yml) + GitHub push-protection.\n# CI is the authoritative backstop; local hooks are developer ergonomics only.\n\nrepos:\n  # Secret detection — catches credentials before they enter git history\n  - repo: https://github.com/gitleaks/gitleaks\n    rev: v8.21.2  # pin to a specific version; keep current via Dependabot\n    hooks:\n      - id: gitleaks\n\n  # Prevent accidentally committing large files (binaries, datasets, etc.)\n  - repo: https://github.com/pre-commit/pre-commit-hooks\n    rev: v5.0.0  # pin; keep current via Dependabot\n    hooks:\n      - id: check-added-large-files\n        args: [\"--maxkb=500\"]\n      - id: check-merge-conflict\n      - id: check-yaml\n      - id: end-of-file-fixer\n      - id: trailing-whitespace\n",
  "setup.md": "<!-- [preset] VALIDATED DEFAULT for the Next.js/TS/Vercel house stack — verify against THIS app; flag any mismatch as a gap in docs/context/07-known-gaps.md. Authority: see citations inline. -->\n\n# SETUP — Getting Started\n\n> Authority: 12-Factor App §II Dependencies, §III Config; Node.js LTS release schedule.\n\n---\n\n## Prerequisites\n\n| Requirement | Version | Check |\n|---|---|---|\n| Node.js | 20.x LTS (see `engines` in `package.json`) | `node --version` |\n| npm | 10.x+ (bundled with Node 20) | `npm --version` |\n| Git | 2.40+ | `git --version` |\n\n---\n\n## Initial Setup\n\n```bash\n# 1. Clone the repo\ngit clone <repo-url>\ncd <repo-name>\n\n# 2. Install dependencies (lockfile-enforced — do NOT use npm install)\nnpm ci\n\n# 3. Copy the environment variable template and fill in values\ncp .env.example .env.local\n# Edit .env.local — never commit this file\n```\n\n---\n\n## Environment Variables\n\nAll required variables are listed in `.env.example` with descriptions but no values.  \nSee `docs/context/06-security.md` for the secrets management policy — in particular:\n\n- No secrets in `NEXT_PUBLIC_*` variables (they are bundled into client JS).\n- Server-side secrets live only in `.env.local` (local) and the deployment platform's secret store (CI/production).\n\n---\n\n## Running Locally\n\n```bash\n# Development server (hot-reload)\nnpm run dev\n\n# Type check\nnpx tsc --noEmit\n\n# Run tests (Vitest)\nnpm test\n\n# Build for production\nnpm run build\n\n# Start production build locally\nnpm run start\n```\n\n---\n\n## Running CI Locally\n\nReplicate the CI pipeline locally before pushing:\n\n```bash\nnpm ci\nnpx tsc --noEmit\nnpm run build\nnpm test\nnpm audit  # advisory — does not fail\n```\n\nFor secret scanning locally, install [gitleaks](https://github.com/gitleaks/gitleaks) and run:\n```bash\ngitleaks detect --source . --verbose\n```\n\n---\n\n## Optional: Claude Code Superpowers Plugin\n\nThe workflow in `docs/context/08-workflow.md` references optional `superpowers:*` skills. To enable them:\n\n```bash\n# Requires Claude Code installed globally\n# See https://github.com/anthropics/claude-code-superpowers for installation\n```\n\nThis is entirely optional — all workflows function without the plugin.\n\n---\n\n## Troubleshooting\n\n| Problem | Fix |\n|---|---|\n| `npm ci` fails | Ensure `package-lock.json` is committed. Delete `node_modules` and retry. |\n| TypeScript errors after `npm ci` | Check Node version matches `engines` in `package.json`. |\n| Missing env vars at runtime | Compare `.env.local` against `.env.example`. |\n| CI fails on type-check locally | Run `npx tsc --noEmit` to see the full error list. |\n",
  "gitignore": "# [preset] VALIDATED DEFAULT for the Next.js/TS/Vercel house stack — verify against THIS app; flag any mismatch as a gap in docs/context/07-known-gaps.md.\n\n# Next.js build output\n.next/\nout/\n\n# Node\nnode_modules/\n\n# Environment variables — NEVER commit these\n.env\n.env.local\n.env.development.local\n.env.test.local\n.env.production.local\n# .env.example IS committed (variable names only, no values)\n\n# Vercel\n.vercel\n\n# TypeScript\n*.tsbuildinfo\nnext-env.d.ts\n\n# Package manager\nnpm-debug.log*\nyarn-debug.log*\nyarn-error.log*\n\n# OS\n.DS_Store\nThumbs.db\n\n# Editor\n.vscode/\n.idea/\n*.swp\n*.swo\n\n# Testing / coverage\ncoverage/\n.nyc_output/\n\n# Misc\n*.log\n",
};
